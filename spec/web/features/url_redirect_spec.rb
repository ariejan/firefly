require 'features_helper'

describe 'URL redirect' do
  # This feature test is useless, because Capybara
  # does not handle redirects to external domains.
  # See spec/web/controller/items/resolver_spec.rb instead.
end
